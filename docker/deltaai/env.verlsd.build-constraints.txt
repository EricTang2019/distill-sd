# Intentionally sparse build-constraint file for source builds inside the
# verlsd sync overlay image.
#
# Runtime packages are still pinned by env.verlsd.constraints.txt. We keep build
# isolation flexible here so sdist-only packages (for example megatron-core)
# can resolve their own build dependencies without conflicting with the runtime
# setuptools pin.
