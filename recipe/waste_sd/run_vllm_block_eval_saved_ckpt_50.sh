#!/usr/bin/env bash
set -euo pipefail

ROOT="$(cd "$(dirname "${BASH_SOURCE[0]}")/../.." && pwd)"
PROJECT_ROOT="$(cd "$(dirname "${BASH_SOURCE[0]}")/../../.." && pwd)"

export WASTE_SD_VLLM_ENV_NAME="${WASTE_SD_VLLM_ENV_NAME:-verlsd312}"
export WASTE_SD_VLLM_TARGET_MODEL="${WASTE_SD_VLLM_TARGET_MODEL:-Qwen/Qwen3-1.7B}"
export WASTE_SD_VLLM_DRAFT_MODEL="${WASTE_SD_VLLM_DRAFT_MODEL:?Set WASTE_SD_VLLM_DRAFT_MODEL to the saved HF ckpt path.}"
export WASTE_SD_VLLM_INPUT_DATA="${WASTE_SD_VLLM_INPUT_DATA:-${PROJECT_ROOT}/data/gsm8k_gkd/test.parquet}"
export WASTE_SD_VLLM_OUTPUT_DIR="${WASTE_SD_VLLM_OUTPUT_DIR:-${ROOT}/outputs/saved_ckpt_eval_50}"
export WASTE_SD_VLLM_RUN_LABEL="${WASTE_SD_VLLM_RUN_LABEL:-saved_ckpt_eval_50}"
export WASTE_SD_VLLM_VISIBLE_DEVICES="${WASTE_SD_VLLM_VISIBLE_DEVICES:-0}"
export WASTE_SD_VLLM_MAX_PROMPTS="${WASTE_SD_VLLM_MAX_PROMPTS:-50}"
export WASTE_SD_VLLM_PARALLEL_MODE="${WASTE_SD_VLLM_PARALLEL_MODE:-dp}"
export WASTE_SD_VLLM_MAX_MODEL_LEN="${WASTE_SD_VLLM_MAX_MODEL_LEN:-4096}"
export WASTE_SD_VLLM_ENFORCE_EAGER="${WASTE_SD_VLLM_ENFORCE_EAGER:-0}"
export WASTE_SD_VLLM_STRICT_DRAFT_PROBS="${WASTE_SD_VLLM_STRICT_DRAFT_PROBS:-1}"
export WASTE_SD_VLLM_TEMPERATURE="${WASTE_SD_VLLM_TEMPERATURE:-1.0}"
export WASTE_SD_VLLM_MAX_CONCURRENT_REQUESTS="${WASTE_SD_VLLM_MAX_CONCURRENT_REQUESTS:-4}"
export WASTE_SD_VLLM_MAX_NUM_BATCHED_TOKENS="${WASTE_SD_VLLM_MAX_NUM_BATCHED_TOKENS:-4096}"
export WASTE_SD_VLLM_MAX_NUM_SEQS="${WASTE_SD_VLLM_MAX_NUM_SEQS:-64}"
export WASTE_SD_VLLM_NUM_SPECULATIVE_TOKENS="${WASTE_SD_VLLM_NUM_SPECULATIVE_TOKENS:-6}"
export WASTE_SD_VLLM_TMP_DIR="${WASTE_SD_VLLM_TMP_DIR:-${TMPDIR:-/tmp}/waste_sd_vllm_${USER}}"

bash "${ROOT}/recipe/waste_sd/run_vllm_block_eval_single.sh"
