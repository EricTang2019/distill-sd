# Known package gaps relative to the local verlsd environment when building an
# arm64 DeltaAI image on top of jingwutang2023/verl-deltaai-verlsd:20260318.
#
# These are intentionally kept out of env.verlsd.sync.requirements.txt until a
# working arm64 installation path exists.
#
# megatron-core is intentionally excluded from this eval-oriented overlay
# because on the current base it forces numpy<2 and conflicts with the existing
# opencv-python-headless dependency chain.
megatron-core==0.13.1
transformer-engine==2.6.0+c90a720
